/*package com.example.ian.treehouseweatherapp;

/**
 * Created by ian on 4/30/2015.

public class DialogConstructor {
    String mAlertTitle;
    String mAlertMessage;
    String mAlertButtonMessage;
    // must think of way to handle multiple Dialog strings, with the same structure as this class.
    DialogConstructor (int errorState){
        //errorState of 1 is for HTTP CallBack error
        //errorState of 2 is for unavailable network error
        Context context =

        if (errorState == 1){
            mAlertTitle = context.getString(R.string.alert_title_http);
            mAlertMessage = context.getString(R.string.alert_message_http);
            mAlertButtonMessage = context.getString(R.string.alert_button_http);
        } else if (errorState == 2) {
            mAlertTitle = context.getString(R.string.alert_title_network);
            mAlertMessage = context.getString(R.string.alert_message_network) +
                    context.getString(R.string.alert_title_message2);
            mAlertButtonMessage = context.getString(R.string.alert_button_network);
        }
    }
}*/